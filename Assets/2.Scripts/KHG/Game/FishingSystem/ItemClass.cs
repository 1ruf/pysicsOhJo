using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemClass : FishingSystemMain
{
    public enum item
    {
        clip = 0,
        vault = 1,
        glassess = 2
    }

    private int num;
    private string name;
    private float price;

    public void SetItem(int ObjectNum)
    {
        switch (item)
        {
            case item.
        }
    }
}
